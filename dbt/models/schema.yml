version: 2

models:
  - name: weather_summary
    description: "Analytics model for weather data"
    columns:
      - name: location_name
        data_tests:
          - not_null
      - name: date
        data_tests:
          - not_null
      - name: temp_mean
      - name: temp_anomaly
